version: '3'

services:
    node-server:
        container_name: dex-backend
        ports:
            - '3000:3000'
        links:
            - mongo
    mongo:
        container_name: mongo
        image: mongo
        ports:
            - '27017:27017'